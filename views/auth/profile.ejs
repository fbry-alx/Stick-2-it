<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://stackpath.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/styles/user.css">
  <title>User Profile</title>
</head>

<body>
  <header class="main-header">
    <a href="/" class="logo">
      <img src="img/logo.png" alt="logo">
    </a>
    <nav class="main-nav">
      <ul>
        <li><a href="/">Log Out</a></li>
      </ul>
    </nav>
  </header>

  <div class="profile">
    <img src="img/photo1.jpg" alt="user" width="150">
    <h3><%=user.username%></h3>
    <p><i class="fa fa-envelope info"></i><%=user.email%></p>
    <hr>
    <% if (!user.habits.length) { %>
    <h3>You aren't tracking any habits yet</h3>
    <% } else { %>
    <p><b>Weekly Habits:</b></p>
    <ul>
      <% user.habits.forEach(habit => { %>
      <li>
        <a href="/habits/<%=habit._id%>"><%=habit.name%></a>
      </li>
      <% }) %>
    </ul>
    <form action="/logout?_method=DELETE" method="POST">
      <input class="log-out-btn" type="submit" value="Log Out">
    </form>

    <%}%>
    <div class="about-par">
      <h5 class="about-title">About</h5>
      <p>Lorem ipsum dolor, sit amet consectetur adipisicing elit. Tenetur reprehenderit repellat delectus. Cum nulla modi veniam in delectus error odio.</p>
    </div>
    </div>
</body>

</html>